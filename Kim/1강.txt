유니티의 인터페이스는
Hierachy(내가 현재 넣은 구성요소들)
Scence(게임 개발하면서 보이는 장면)
Game(실제로 게임을 실행하면 보이는 화면)
Inspector(Hierachy에서 선택한 요소의 구성요소를 표시해주는 영역)
따로 CS script에서 [SerializeField] 속성을 통해서 직접적으로 값을 입력해줄 수도 있다.
추가적으로 원하는 구성요소를 가져오는 기능도 가지고 있는데
주로 Collider를 통해 객체에게 hit box를 설정해주거나 
Rigid body를 통해 해당 물체에게 관성/중력같은 물리적인 값을 넣어주는 등 여러가지 기능을 추가할 수 있다.
Project(내가 현재 구성하고 있는 Unity프로그램 내부에 들어가있는 파일을 보여준다)
여기서 기본적으로 제공해주는 파일인 Prefeb은 객체를 클래스(대충 붕어빵과 붕어빵틀의 관계)로 만들 수 있는데
예를 들어서 반복적으로 나오는 몬스터,기본공격의 모션등의 객체를 손쉽게 찍어낼 수 있게 해주는 요소이다.
Console(쉽게 말하자면 프로그램을 실행했을 때 출력되는 문자열을 보여주고, 프로그램의 문제가 있다면 그 문제가 뭔지 보여주는 기능을 한다.)
