게임오브젝트가 실행되는 순서
    1.초기화 영역
1-1. Awake
게임 오브젝트를 생성할 때, 딱 한 번 실행한다.
1-2. Start
업데이트 영역으로 가기전에, 최초로 실행된다.
//여기에 활성화 버튼이 들어간다.//
    onEnable함수가 있는데 얘는 해당 게임 오브젝트가 활성화 될때마다 실행된다.
    2. Frame 영역 (1초당 몇 프레임할 때 그 프레임)
2-1. FixedUpdate(물리적인 영역) 
물리 연산을 실행하기 전에 실행되는 업데이트 함수다.
얘는 컴퓨터의 성능에 상관없이 실행주기가 고정(ex:60프레임)되기 때문에
 많이 쓰면 CPU의 부하를 야기할 수 있다.
2-2. Update(게임 로직)
물리 연산에 대한걸 제외한 나머지 연산을 실행하는데,
얘는 컴퓨터의 성능에 따라서 실행주기를 조절한다.
2-3. LateUpdate
모든 업데이트 영역이 끝난 후 실행되는데 카메라 or 후처리가 실행되는 영역이다.
//여기에 비활성화 버튼이 들어간다.//
    ondisable함수가 있는데 얘는 게임 오브젝트가 비활성화 될때마다 실행된다.
    3. onDestroy영역
여기는 Awake랑 반대가 되는 영역으로 존재하는 데이터를 해제하고 프로그램을 종료시킬 로직을 실행한다.

